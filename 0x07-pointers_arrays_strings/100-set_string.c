#include "main.h"
#include <stdio.h>

/**
 * set_string - Ecrire une fonction
 * qui définit la valeur d’un pointeur à un char.
 * @s: variable
 * @to: variable
 */

void set_string(char **s, char *to)
{
		*s = to;
}
