#include "main.h"
#include <stdio.h>

/**
 * infinite_add - Transforme les lettres en language rot13
 * @n1: variable
 * @n2: variable
 * @r: variable
 * @size_r: variable
 * Return: (n)
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	int i = 0;
	int j = 0;
	
	while(i <= size_r || j <= size_r)
	{

	}
}
